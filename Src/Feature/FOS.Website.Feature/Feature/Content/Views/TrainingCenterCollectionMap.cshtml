@using Sitecore.Mvc
@using FOS.Website.Feature.Content.Models
@using DynamicPlaceholders.Mvc.Extensions
@using Sitecore.Configuration
@using Sitecore.Links
@using Sitecore.Mvc.Presentation
@using Synthesis.Mvc.Helpers
@model TrainingCenterCollectionMapModel

@if(!@Model.MapNodes.Any())
{
    return;
}

<div class="map__container">
    <!--TABS - VISIABLE LARGE UP -->
    <ul class="tabs" data-js-tabs="map">
        @* TODO - remove hardcoded text for Lista & Karta *@
        <li class="tabs__title active"><a href="#panel1">Lista</a></li>
        <li class="tabs__title"><a href="#panel2" data-js-map-show>Karta</a></li>
    </ul>
    <!--MAP - VISIBLE ALL THE TIME -->
    <div class="tabs__content visible-medium-up--block" id="panel2" data-js-tabs-map>
        <div id="map"></div>
    </div>
    <!--MAP-LIST - VISIBLE ALL THE TIME -->
    <div class="tabs__content map__content" id="panel1" data-js-tabs-map>
        <div class="map__list">
            
            <h3>@Html.TextFor(m => m.MapWidgetItem.MapTitle)</h3>
            <ul>
                @{
                    int i = 0;
                    foreach (var mapNode in Model.MapNodes)
                    {
                        <li data-js-map-i="@i" data-js-map-title="@mapNode.MapHeadline" data-js-map-address="@mapNode.Street @mapNode.StreetNr, @mapNode.City" data-js-map-url="@LinkManager.GetItemUrl(mapNode.InnerItem)" data-js-map-lat="@mapNode.Latitude" data-js-map-lng="@mapNode.Longitude">
                            @* TODO - remove hardcoded text for Lista & Karta *@
                            <button class="map__list--show">Visa på karta</button>
                            <a href="@LinkManager.GetItemUrl(mapNode.InnerItem)">
                                <h4>@mapNode.MapHeadline</h4>
                                <address>@mapNode.Street @mapNode.StreetNr, @mapNode.City</address>
                            </a>
                        </li>
                        i = i + 1;
                    }
                }
            </ul>
        </div>
    </div>
</div>

